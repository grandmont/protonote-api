version: "3.2"

services:
  app:
    build: .
    container_name: api
    ports:
        - '4000:4000'
    command: node dist
    environment:
        NODE_ENV: production
        PORT: 4000
        DATABASE_URL: postgresql://postgres:postgres@database-1.c2zncw873tim.us-east-1.rds.amazonaws.com/protonote?schema=public
        ACCESS_TOKEN_PRIVATE_KEY:
        ACCESS_TOKEN_PUBLIC_KEY:
        REFRESH_TOKEN_PRIVATE_KEY:
        REFRESH_TOKEN_PUBLIC_KEY:
        SPOTIFY_CLIENT_ID:
        SPOTIFY_CLIENT_SECRET: