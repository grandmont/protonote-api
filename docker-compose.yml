version: "3.1"

services:
  app:
    build: .
    container_name: api
    ports:
        - '4000:4000'
    command: yarn start
    environment:
        NODE_ENV: production
        PORT: 4000
    env_file:
      - .env